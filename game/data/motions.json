{
  "_comment": "MotionForge rows — tuned overrides for the procedural sin() rigs. The engine folds these over its stock literals at boot (additive: only listed params change). Edit at http://localhost:8896 or via the REST API (GET /api/help).",
  "rigs": {
    "puppet": {
      "gait": {
        "arm_swing": 1.05,
        "stride_amp": 0.72,
        "step_bob": 0.15,
        "cadence_speed": 1.35,
        "lean_turn": 0.26
      }
    }
  }
}
