# UI/UX Plan → consolidated into MASTER_PLAN.md

The plan from the side chat was expanded into a full 5-goal build and saved as
**[MASTER_PLAN.md](MASTER_PLAN.md)** (data spine, MapForge v2, Vehicle Forge, the
Divided States lore bible, and the SoundForge SFX batch). All five goals are built,
tested, and committed. The two editor tools that carry the UI/UX work:

- **Vehicle Forge** (`tools/vehicleforge`, `:8898`) — the AAA fleet editor: armor
  gauges front-and-center, live stat sliders, a sortable fleet compare panel.
- **MapForge v2** (`tools/mapforge`, `:8899`) — the world editor: authored-placement
  layer, auto off-ramps/exits, and a town-template stamper.

See MASTER_PLAN.md for the goal-by-goal detail and proof cases.
