# Auto Claude data directory
.auto-claude/
unity/

# Secrets — never commit API keys
.env
tools/soundforge/.env
game/data/laptimes.json

# packaged builds (BUILD.bat output) - never commit exes
build/
